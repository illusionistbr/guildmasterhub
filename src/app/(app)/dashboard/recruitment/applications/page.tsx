import { ComingSoon } from "@/components/shared/ComingSoon";
import { FileText } from "lucide-react";

export default function ApplicationsPage() {
  return <ComingSoon pageName="Candidaturas de Membros" icon={FileText} />;
}
